{
  "name": "pw-demo",
  "private": true,
  "devDependencies": {
    "@playwright/test": ""
  },
  "scripts": {
    "test": "playwright test",
    "test:docker": "docker compose run --rm pw",
    "record": "docker compose run --rm --service-ports pw_record",
    "report": "playwright show-report"
  }
}
